Fix `pdm install` not responding to Ctrl+C: interrupting the command now cancels the installs that haven't started, lets the running ones finish, and exits with code 130 instead of printing a traceback.
